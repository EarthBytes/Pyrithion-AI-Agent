# Credentials

Do not commit real keys to git.

1. Copy the example file:
   ```bash
   cp credentials/google-service-account.json.example credentials/google-service-account.json
   ```
2. Replace placeholders with your Google Cloud service account JSON.
3. Share your Drive folder with the service account email (Viewer access).
